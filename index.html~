<!doctype html>
<html ng-app="Todo">
<head>
  <title>TODO POUCH</title>
  <link rel="stylesheet" href="/components/bootstrap/css/bootstrap.css">
</head>
<body>
  <div class="container">
    <h1>TODO POUCH</h1>

    <!-- Explaining the span 

          This shows how many todos have not been completed
          vs the number of todos in total.  This is done with
          a ng-bind directive to call $scope.remaining() function
          to see how many todos are unfinished.  The we use ng-bind
          to call $scope.todos.length see the total count of todo items
    -->
  
    <span>{{remaining()}} of {{todos.length}} remaining</span>

    <!-- Removing todos

          This allows the user to remove all done tasks from the list.
          It is done with an anchor element and an ng-click attribute
          to map the anchor click event to the $scope.removeDone function.
    -->

    [<a ng-href="" ng-click="removeDone()">Remove done</a>]

    <!--  List of todos

           This makes an unordered list of the todos.  Each item is
           given a check box with an attribute ng-model that is assigned
           to $scope.todo.done and then maps the input ng-click attribute 
           to $scope.updateTodo(todo).  Then the ng-class directive adds
           the done class to the span elemnt if todo.done === true
           The ng-bind directive shows the todo task
    -->

    <ul class="unstyled">
      <li ng-repeat="todo in todos">
        <input type="checkbox" ng-model="todo.done">
        <span ng-class="{{done: todo.done">{{todo.text}}</span>
      </li>
    </ul>

    <!--  The form  
  
           A form is added with an ng-submit directive, it is assigned
           to $scope.addTodo().  In the form ng-model is assigned to 
           $scope.todoText.
     -->    

    <form ng-submit="addTodo()">
      <input type="text" ng-model="todoText" size="30"
          placeholder="add new todo here">
      <input class="btn btn-primary" type="submit" value="add">
    </form>

    <ng-view></ng-view>
  </div>
  <script src="/components/jquery/jquery.js"></script>
  <script src="/components/angular/angular.js"></script>
  <script src="/components/pouchdb-nightly.min/index.js"></script>
  <script src="/app/app.js"></script>
</body>
</html>

